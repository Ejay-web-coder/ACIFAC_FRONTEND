# Public URL of the ACIFAC Express API (no trailing slash). Built into the bundle,
# so never put secrets in VITE_* variables.
# Leave empty in local development: Vite proxies /api to http://localhost:4000.
VITE_API_URL=https://your-acifac-backend.onrender.com
